#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    int n; cin >> n;
    map<string>s;
    for(auto it: s) {
        cin >> it;
        s.insert(it);
        map[s]++;
    }

    string ans;
    for(char c: s){
        if(s > 2)
            ans+=
    }
    if(s > 2)
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    solve();
    return 0;
}

//Challenge:

//

//Observation:

//

//Analysis:

//